# Ссылки по источникам (ac1, ac2)

Один бот — разные ссылки для разных аккаунтов. Так можно считать, сколько человек зашло с каждого аккаунта.

## Как сделать ссылки

Бот по умолчанию: **@risetestq_bot**. Имя задаётся в `.env` как `BOT_USERNAME`.

- **Аккаунт 1 (ac1):**  
  `https://t.me/risetestq_bot?start=ac1`

- **Аккаунт 2 (ac2):**  
  `https://t.me/risetestq_bot?start=ac2`

Эти же ссылки админ видит в боте в разделе «Реферал.ссылки».

В Reels/сторис первого инстаграм-аккаунта вставляешь ссылку с `ac1`, во втором — с `ac2`.  
Пользователь переходит по ссылке → открывается бот с параметром `start=ac1` или `start=ac2` → бот сохраняет этот параметр как «источник».

## Где смотреть

- **Статистика** (кнопка «Статистика» у админа) — блок «По источникам (ссылка)»: ac1 — N, ac2 — M.
- **Поиск по номеру** — в карточке участника есть поле «Источник».
- **Выгрузка Excel** — колонка «Источник».

Можно использовать любые метки вместо ac1/ac2, например `reels1`, `story2`, главное — один короткий параметр после `start=`.
